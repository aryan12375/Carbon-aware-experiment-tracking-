# EcoTrack — Green MLOps Framework
# Core dependencies for tracker_utils.py + check_gate.py
# Install with: pip install -r requirements.txt

# ── Carbon tracking ───────────────────────────────────────────────────────
codecarbon>=2.4.1            # GPU/CPU power instrumentation
requests>=2.31.0             # CO2Signal + ElectricityMaps API calls

# ── GPU metadata ──────────────────────────────────────────────────────────
pynvml>=11.5.0               # NVIDIA GPU metadata (name, TDP, memory)
                             # Optional — tracker degrades gracefully without it

# ── Backend (Phase 2 — FastAPI) ───────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0            # async SQLite driver (required for DATABASE_URL sqlite+aiosqlite)
alembic>=1.13.0              # database migrations
pydantic>=2.7.0
pydantic-settings>=2.2.0

# ── Feature deps ──────────────────────────────────────────────────────────
reportlab>=4.1.0             # AI Nutrition Label PDF generation (Feature 2)

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
httpx>=0.27.0                # async test client for FastAPI

# ── Dev / linting ─────────────────────────────────────────────────────────
ruff>=0.4.0
black>=24.4.0

